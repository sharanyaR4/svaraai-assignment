/** @type {import('next').NextConfig} */
const nextConfig = {
  // Remove this line: experimental: { appDir: true },
  
  // Keep your other config options if you have any
};

module.exports = nextConfig;